# 更新日志

本文件记录 C.le.控制台的重要功能变化。正式发布版本使用语义化版本号；尚未生成安装包的源码更新归入“未发布”。

## 未发布

## 1.1.27

### 独立多模型 API 控制台

- 多模型 API 服务改为独立顶层窗口；主控制台入口只负责唤起或聚焦该窗口，不再把完整服务页面混入账号管理主界面。
- 独立窗口关闭后仅隐藏界面，已启用的 API sidecar 和网关继续运行；主窗口关闭时也不会误杀正在运行的独立服务或网页创作工作台。
- 非活动主窗口停止账号与额度自动刷新，重新获得焦点后再恢复调度；隐藏的多模型窗口不执行后台 UI 状态刷新。
- 多模型账号卡片新增额度用量：Grok 显示套餐桶，Antigravity 与 Gemini 显示各模型剩余/已用百分比，Claude 显示 5 小时、7 天及 Sonnet 周额度；重置时间统一按北京时间展示。

### Codex / GPT 账号彻底分离

- 多模型 API 账号同步不再读取 Codex 账号，也不再发布 Codex 模型或 `codex-api-key` 路由。
- 启动时自动清理旧版本遗留在多模型账号池中的 Codex、Chat2API 与 Aurora GPT 账号，并同步清除下游 Key 对这些旧账号的绑定。
- Codex 凭证刷新、Chat2API/Aurora 本地账号管理继续保留在独立的 Codex API 服务页面，不再提供“接入多模型网关”入口。

### 验证

- TypeScript 类型检查、Vite 生产构建通过。
- Rust 编译检查通过；多模型 API 定向测试 12 项通过，包含 Codex 账号自动隔离回归测试。

## 1.1.26

### 多模型 API 分流修复

- 修复“Round Robin 轮询”实际上仍被默认一小时会话固定覆盖的问题；旧版本默认开启的会话固定会自动迁移为关闭，避免连续请求长期压在同一个 Antigravity 账号上。
- 修复所有模型共用一个全局轮询游标的问题，改为按“供应商 + 模型 + 下游 Key”维护独立游标，并直接轮转该模型实际可用的账号集合。
- 接入 Antigravity 每模型实时剩余额度：智能轮询会优先在最高额度附近的健康账号之间分流，当前 Claude 仅剩约 7% 的账号不会在其他兼容账号仍有 100% 时继续被选中。
- 新增稀疏账号池、不同模型独立游标、低额度避让三组 sidecar 回归测试；设置页明确提示会话固定会造成单号集中消耗。

### 验证

- Go sidecar 全量测试通过。
- TypeScript 类型检查通过。
- Rust 多模型 API 定向测试 12 项通过。

## 1.1.25

### 网页创作工作台

- 修复豆包网页在 Chromium/WebView2 下对大图执行客户端重压缩的问题。豆包当前线上脚本会把超过 10/15MiB 的图片压缩到最高 2560 像素；工作台现在仅对豆包账号启用其网站已有的 Apple WebKit 原图分支，支持的图片在 30MiB 内直接上传原文件字节，不再经过网页端二次缩放编码。
- 独立账号窗口同步启用原图上传保护与原生 HTML5 文件拖放；未修改系统、WinINET 或 WinHTTP 代理配置。
- 网页工具栏新增“独立窗口”：将当前账号、当前地址和同一份独立浏览器 Profile 转移到顶层窗口。网页创作工作台随后可以完全关闭，账号独立窗口仍会继续运行。
- 在工作台素材区显示豆包原图保护状态，避免把平台脚本的快速压缩上传误认为原文件上传。

### 验证

- Rust 编译检查覆盖独立窗口转移命令，单元测试验证原图保护脚本只注入豆包 WebView。
- TypeScript 类型检查与生产构建通过。

## 1.1.24

### Grok / xAI 多账号导入

- 新增 Sub2API `sub2api-data` 导出 JSON 解析，支持凭据位于 `accounts[].credentials`、camelCase/snake_case Token 字段的结构。
- 新增“账号 + 密码 + refresh_token”批量文本导入，每行一个账号，兼容 `----`、`|`、Tab、`::`、分号和逗号分隔。
- 支持只有 `refresh_token` 的 Grok 账号：导入后通过 xAI OAuth token endpoint 自动换取 `access_token`，再执行额度检测。
- Grok 文件选择器扩展为 JSON、TXT 和 CSV；导入界面补充明确格式示例。
- 账号密码仅作为本地迁移/恢复元数据保存，不会作为 xAI API 请求字段发送。

### 网页创作工作台

- 修复 Windows 下 Tauri 原生文件拖放处理器抢先消费系统 Drop，导致豆包网页聊天框收不到外部图片/文件的问题；账号内嵌 WebView 现在直接使用 WebView2 的 HTML5 `DragEvent` / `DataTransfer` 文件拖放。
- 新增 C.le 豆包账号凭证 v1 的批量导出/导入接口和工作台按钮，可将 Cookie、域、路径、过期时间、HttpOnly、Secure 与 SameSite 属性迁移到另一台电脑的独立账号 Profile。
- 导入文件会校验格式、账号数量、Cookie 域名和字段长度；仅接受 `doubao.com` 子域，首次打开后自动写入 WebView 并向豆包服务端验证；验证成功后删除明文引导副本，后续由独立浏览器 Profile 持久化，避免旧 Cookie 覆盖新会话。
- 显式退出或删除账号时同步清除本地可迁移凭证，避免用户退出后被旧凭证自动恢复。

### 验证

- Rust 定向测试覆盖官方 Grok CLI registry、Sub2API 嵌套凭据和两种账号密码 RT 文本格式。
- TypeScript 类型检查通过。

## 1.1.23

### 修复

- 修复 Antigravity 账号缺少 `project_id` 且上游网络不可用时，多模型 API sidecar 在读取本地凭据阶段被同步网络请求阻塞，最终报 `runtime startup timeout`、网关无法监听端口的问题。
- 网关启动时的凭据加载现在严格保持为本地磁盘操作；项目识别和令牌刷新继续由独立账号刷新流程处理，不再拖死整个网关。

### 验证

- 使用两个缺少 `project_id` 的真实 Antigravity 凭据验证 sidecar 可正常启动。
- `GET /healthz` 返回 `service=cle-cliproxy,status=ok`，`GET /v1/models` 返回 38 个模型。

## 1.1.22

### 多模型 API / Grok

- 新增 Grok CLI/xAI 多账号管理：使用官方 OIDC Device Flow 逐个授权，账号凭据独立保存并接入现有轮询、冷却、重试和故障转移。
- 支持从本机 `~/.grok/auth.json`、`GROK_HOME/auth.json` 或官方 registry JSON 导入 Grok CLI 登录态，按账号身份去重，不覆盖其他供应商账号。
- 新增 Grok 账号批量额度刷新，展示套餐、每周额度、产品额度、任务额度、重置时间和 Grok Code 可用状态。
- xAI access token 到期前自动刷新并保存 refresh-token rotation；额度查询失败时保留上次成功缓存，同时显示需重登或网络错误。
- Grok OAuth、额度查询沿用程序当前代理配置或只读检测到的系统代理，不修改系统/应用代理设置。
- 参考 Cockpit Tools 的账号管理交互与数据口径，后端实现保持在 C.le. 现有 CLIProxyAPI 账号池内，不新增第二套网关。

## 1.1.14

### 优化

- macOS 主窗口、状态窗和内容卡片统一为深色半透明 Liquid Glass 质感，可透出桌面壁纸。
- 补齐窄窗口文字换行与长文本保护，并让网页创作工作台的控件风格与主界面一致。

### 修复

- 修复 macOS 原生全屏进入独立黑色空间的问题：全屏操作会保留在可透出桌面的最大化窗口模式。
- 修复状态窗外层出现直角底板的问题，改用原生圆角裁剪；黄色最小化按钮会自动显示状态窗。
- 在“出口监测”的实时路由拓扑卡片中增加“简窗 / STATUS”入口，可主动打开状态窗。

### 验证

- TypeScript 类型检查：通过。
- Rust `cargo check --locked`：通过。
- macOS `.app` 打包、签名校验及实际桌面状态窗截图：通过。

## 1.1.8

### 修复

- 修复 macOS 额度球标题栏拖拽命中区域覆盖操作按钮，导致按钮无法点击的问题。
- 在额度球中补充独立的“窗口化”和“简模式”操作，并让简模式、昼夜主题在额度球与主窗口间同步。
- 撤回 1.1.7 因目标理解偏差而加入主页面的额外“窗口化”按钮；主窗口原生红黄绿按钮避让仍保留。

### 验证

- TypeScript 类型检查：通过。
- 前端生产构建：通过。
- macOS 源码就绪检查：通过。
- Rust `cargo check --locked`：通过，保留现有未使用代码警告。

## 1.1.7

### 修复

- 修复 macOS 顶部透明拖拽层遮挡“简模式”和日间/夜间切换按钮的问题。
- 为 macOS 顶部区域避让原生红黄绿窗口按钮，恢复其可靠点击范围。
- 新增 macOS“窗口化 / 最大化”快捷按钮；处于全屏时可直接退回窗口模式。

## 1.1.6

### 新增

- 发布 Windows x64 与 macOS Apple Silicon 双平台安装包。
- 建立 GitHub Release 签名更新链路，同时生成两平台 `latest.json` 更新清单。
- macOS 客户端现在会检查新版本并支持应用内下载更新。

### 修复

- 修复更新构建未注入公钥，导致 Tauri 打包报 `missing field pubkey` 的问题。
- 修复 GitHub Release 缺少签名包和 `latest.json`，导致应用内更新地址返回 404 的问题。
- 修复 Antigravity 不可调用模型被重复加入模型目录的问题。

### 新增

- 新增即梦 API 服务管理页面和 Rust 后端服务模块。
- 新增即梦无限画布页面、前端服务封装和 Tauri 状态命令。
- 新增豆包 Seedance 请求适配及对应 Go 测试。
- 新增 Codex agent identity 处理逻辑。
- 新增鱼形鼠标点击吐泡泡互动。
- 新增统一帧率治理工具 `frameGovernor`。
- 新增液态玻璃 2026 视觉样式和统一 UI 适配层。

### 优化

- 将开屏问候移动到轻量首屏入口，使主应用动态加载期间不再只显示黑色背景。
- 调整开屏文案为完整自然句，保留“才酷”作为名字并按语境组织语序。
- 简化开屏文字动画，减少逐字动画带来的生硬感。
- 优化账户页、状态窗、日志筛选和多模型 API 页的响应式布局。
- 账户页选中按钮改为静态高光，同时保留悬停反馈。
- 简模式保留鱼形鼠标主体，移除高成本拖尾。
- 页面失焦、隐藏或进入简模式时降低 WebGL、粒子和持续动画刷新频率。
- 改进多模型 API 路由、媒体端点和本地访问状态处理。

### 修复

- 修复额度球持续闪烁及鼠标样式不同步的问题。
- 修复昼间主题局部色彩不统一、文字对比度不足的问题。
- 修复模型矩阵中央模糊层影响内容清晰度的问题。
- 修复部分窗口尺寸下文字重叠、卡片溢出和工具栏错位的问题。
- 修复开屏前半段长时间黑屏的问题。

### 验证

- 前端生产构建：通过。
- Rust `cargo check`：通过，保留现有未使用代码警告。
- `cle-cliproxy` Go 测试：通过。

## 1.1.4

### 主要内容

- 建立基于 Tauri 2、React 19、Rust 与 Go 的桌面控制台架构。
- 提供多平台 AI 账户与额度管理。
- 提供本地 API、模型路由、状态监测和桌面应用集成。
- 提供昼夜主题、仪表盘、账户工作区与自适应状态窗口。
- 发布 Windows x64 安装版与便携版。

### 说明

`v1.1.4` 是当前稳定发布版本。未发布章节中的功能已经进入 `main` 源码，但在完成全量 Tauri 打包和运行验证前，不视为新的正式安装版本。
